# MindAura AI Backend - Python Dependencies

# ── Web Framework ──────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9   # Required for UploadFile support

# ── Machine Learning (TensorFlow CPU) ─────────
tensorflow-cpu>=2.20.0   # CPU-only build — avoids heavy CUDA packages on Render
numpy>=1.26.4            # TF 2.20+ will pull a compatible version automatically

# ── Image Processing ───────────────────────────
opencv-python-headless>=4.9.0.80   # Headless: no GUI deps (Render-compatible)

# ── Audio Processing ───────────────────────────
librosa>=0.10.2
soundfile>=0.12.1        # Backend codec used by librosa
SpeechRecognition>=3.10.4  # Google STT for voice branch (imported as speech_recognition)

# ── NLP / Text Model (HuggingFace RoBERTa) ────
transformers>=4.41.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.6.0           # CPU-only wheel fetched via extra-index-url above
safetensors>=0.4.3     # Required for loading model.safetensors weights

# ── HTTP Client (optional) ────────────────────
httpx>=0.27.0

# ── Authentication & Database ─────────────────
motor>=3.4.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.1
email-validator>=2.1.1
PyJWT>=2.8.0
